import { Routes } from '@angular/router';
import { PageRelatorio } from '../relatorio-com-progresso/page';

export const routes: Routes = [
  {
    path: 'relatorio',
    component: PageRelatorio
  }


];
